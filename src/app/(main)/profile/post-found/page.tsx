import { ProfileLayout } from "@/components/profile/profile-layout";
import { PostFoundForm } from "./pageClient";

export default function PostFoundPage() {
  return (
    <ProfileLayout>
      <PostFoundForm />
    </ProfileLayout>
  );
}

